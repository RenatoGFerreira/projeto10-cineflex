import { ScreenContainer, Poster, Title } from "./StyleFooter"

export default function Footer(){
    return(
        <ScreenContainer>
            <Poster>
                a esquerda
            </Poster>
            <Title>
                a direita
            </Title>
        </ScreenContainer>
    )
}